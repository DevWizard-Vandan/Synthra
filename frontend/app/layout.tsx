import type { Metadata, Viewport } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";
import { AppShell } from "@/components/layout/app-shell";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const metadata: Metadata = {
  title: "SYNTHRA | Operations Center",
  description:
    "Autonomous quantitative research operations center for WorldQuant BRAIN — real-time campaign monitoring, simulation tracking, and alpha candidate management.",
  keywords: ["SYNTHRA", "WorldQuant", "BRAIN", "alpha research", "quant"],
};

export const viewport: Viewport = {
  colorScheme: "dark",
  themeColor: "#0a0e1a",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable} suppressHydrationWarning>
      <body className="min-h-screen bg-background text-foreground antialiased">
        <Providers>
          <AppShell>{children}</AppShell>
        </Providers>
      </body>
    </html>
  );
}
